kind: Service
apiVersion: v1
metadata:
  name: {{name}}
  namespace: {{namespace}}
  labels:
    {{selector}}: {{uname}}
spec:
  type: NodePort
  selector:
    {{selector}}: {{uname}}
  ports:
{%- for port_info in ports %}
{%- for port, port_name in port_info.items() %}
    - name: {{port_name}}
      protocol: TCP
      port: {{port}}
{%- endfor %}
{%- endfor %}